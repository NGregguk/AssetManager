@model asset_manager.Models.Vendor
@{
    ViewData["Title"] = "Edit vendor";
}

<section class="page-header">
    <div>
        <h1>Edit vendor</h1>
        <p class="muted">@Model.Name</p>
    </div>
    <div class="header-actions">
        <a class="button ghost" asp-area="Admin" asp-controller="Vendors" asp-action="Index">Back to vendors</a>
    </div>
</section>

<div class="panel">
    <form asp-action="Edit" method="post">
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-grid">
            <div class="form-field">
                <label asp-for="Name"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-actions">
            <button type="submit" class="button">Save</button>
            <a class="button ghost" asp-area="Admin" asp-controller="Vendors" asp-action="Index">Cancel</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
